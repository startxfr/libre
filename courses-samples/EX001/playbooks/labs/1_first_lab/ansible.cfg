[defaults]
inventory = inventory
remote_user = student
host_key_checking = False

vault_password_file = resources/secret.txt

[privilege_escalation]
become = True
become_method = sudo
become_user = root
become_ask_pass = False
