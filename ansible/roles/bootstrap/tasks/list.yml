---
- name: "list : Generate bootstrap list report at {{ r_libre_result_file }}"
  template:
    src: libre_report-list.j2
    dest: "{{ r_libre_result_file }}"
#    owner: root
    group: "{{ r_libre_group }}"
    mode: 0666