## LIBRE Session role default variables

do: load

# Instructor directory inside the course directory
session_dir_course_playbooks: "{{ training_dir_course_playbooks | default('playbooks/instructor') }}"

# Course directory used for delivering training
session_dir_course: "{{ training_dir_course | default('/usr/share/libre/course') }}"

# Config file for session
session_config_file: "{{ libre_config_file_session | default('session.json') }}"

# Config file for session
libre_myconfile_session: 'session.yml'
# Default config_session structure
libre_session_type: 'sample'
libre_session_gapi: 'https://script.google.com/macros/s/XXXXXXX_DOCKEY_XXXXXXXXXXXXXXXXXX/exec'