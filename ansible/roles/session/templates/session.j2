---
# session default configuration - libre v{{ libre_version }}

# Default session backend to use (should be one of 'gapi', 'manual' or 'sample')
type: "{{ config_session.type }}"
# If session backend is 'gapi' you should define here your google script backend URL
gapi: "{{ config_session.gapi }}"
