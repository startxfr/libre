---
instructor:
  hosts:
    instructor.{{ libre_session.id }}.{{ libre_infra.route53.domain }}:

classroom:
  children:
    instructor:
  vars:
    ansible_connection: ssh
    ansible_user: "{{ libre_infra.ssh.user }}"
    ansible_ssh_private_key_file: "{{ libre_infra.ssh.privateKey_file }}"
