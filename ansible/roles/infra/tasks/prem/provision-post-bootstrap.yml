--- 
- name: "prem : provision-post-bootstrap : Log bootstrap post-provision action"
  include_role:
    name: libre
    tasks_from: lib/log
  vars:
    log_name: "On-Premise infrastructure is Post-provisioned on bootstrap"
    log_component: infra-prem
    log_action: post-provision on-premise infrastructure on bootstrap
  run_once: true
