---
- name: "Install instructor environment on instructor host"
  hosts: 'instructor'
  gather_facts: true

  roles:
    - role: instructor
      do: load
      libre_type: instructor
    - role: instructor
      do: install
      libre_type: instructor
