---
- name: "Display instructor history on instructor"
  hosts: 'instructor'
  gather_facts: true
  user: root

  roles:
    - role: instructor
      do: load
      libre_type: instructor
    - role: instructor
      do: history
      libre_type: instructor
      history_filter: all
