---
- name: "Check instructor environment on {{ target }}"
  hosts: '{{ target }}'
  gather_facts: true
  user: root

  roles:
    - { role: instructor, do: load }
    - { role: instructor, do: status }
