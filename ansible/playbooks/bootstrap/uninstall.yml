---
- name: "Uninstall bootstrap environment on bootstrap"
  hosts: 'bootstrap'
  gather_facts: true
  user: root

  roles:
    - role: bootstrap
      do: load
      libre_type: bootstrap
    - role: bootstrap
      do: uninstall
      libre_type: bootstrap
      libre_uninstall_deep: false
