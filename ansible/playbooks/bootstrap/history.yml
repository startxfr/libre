---
- name: "Display bootstrap history on bootstrap"
  hosts: 'bootstrap'
  gather_facts: true
  user: root

  roles:
    - role: bootstrap
      do: load
      libre_type: bootstrap
    - role: bootstrap
      do: history
      libre_type: bootstrap
      history_filter: all
