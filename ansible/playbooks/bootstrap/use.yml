---
- name: "Use a new session context on bootstrap host"
  hosts: 'bootstrap'
  gather_facts: true
  user: root

  roles:
    - role: bootstrap
      do: load
      libre_type: bootstrap
    - role: bootstrap
      do: use
      libre_type: bootstrap
