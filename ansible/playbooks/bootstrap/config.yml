---
- name: "Configure bootstrap environment on bootstrap"
  hosts: 'bootstrap'
  gather_facts: true

  roles:
    - role: bootstrap
      do: load
      libre_type: bootstrap
    - role: bootstrap
      do: config
      libre_type: bootstrap
