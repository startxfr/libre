---
- name: "Check bootstrap environment on bootstrap"
  hosts: 'bootstrap'
  gather_facts: true
  user: root

  roles:
    - role: bootstrap
      do: load
      libre_type: bootstrap
    - role: bootstrap
      do: status
      libre_type: bootstrap
