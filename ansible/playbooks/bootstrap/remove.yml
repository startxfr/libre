---
- name: "Remove bootstrap tools on bootstrap"
  hosts: 'bootstrap'
  gather_facts: true
  user: root

  roles:
    - { role: bootstrap, do: load }
    - { role: bootstrap, do: uninstall, libre_uninstall_deep: true }
