---
- name: "Uninstall training repository environment on instructor host"
  hosts: 'instructor'
  gather_facts: true
  user: root
  become: yes

  roles:
    - role: repository
      do: load
    - role: repository
      do: uninstall
#      libre_uninstall_deep: true
