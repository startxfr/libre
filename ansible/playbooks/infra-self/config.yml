---
- name: "Configure classroom self infrastructure on bootstrap host"
  hosts: 'bootstrap'
  gather_facts: true

  roles:
    - role: infra
      do: load
      infra_type: self
    - role: infra
      do: config
      infra_type: self
