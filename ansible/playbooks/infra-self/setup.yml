---
- name: "Setup Self-Hosted Classroom infrastructure"
  hosts: 'local'
  gather_facts: true
 
  roles:
    - { role: session, do: load }
    - { role: session, do: install-postcheck }
    - { role: infra_self, do: load }
    - { role: infra_self, do: setup }
