---
- name: "Post-Provision Self-Hosted Classroom infrastructure"
  hosts: '{{ target }}'
  gather_facts: true
 
  roles:
    - { role: session, do: load }
    - { role: session, do: install-postcheck }
    - { role: infra_self, do: "load" }
    - { role: infra_self, do: "provision-post" }