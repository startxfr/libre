---
- name: "Provision classroom infrastructure on bootstrap host"
  hosts: 'bootstrap'
  gather_facts: true

  roles:
    - role: infra
      do: load
      infra_type: prem
    - role: infra
      do: provision
      infra_type: prem
