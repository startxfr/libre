---
- name: "Deprovision classroom infrastructure on bootstrap host"
  hosts: 'bootstrap'
  gather_facts: true

  roles:
    - role: infra
      do: load
      infra_type: aws
    - role: infra
      do: deprovision
      infra_type: aws
