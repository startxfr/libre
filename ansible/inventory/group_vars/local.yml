## default configuration vars for local group
---
# use a local connection to reach this host
ansible_connection: local
# Enable verbose configuration
libre_verbose: true
# Enable debug configuration
libre_debug: true
# Workstation type to install
libre_type: student
# Default user for course content
libre_user: student
# playbooks directory inside the course directory
training_dir_course_playbooks: playbooks/student
