---
local:
  hosts:
    localhost:
  vars:
    ansible_connection: local

bootstrap:
  hosts:
    localhost:
  vars:
    ansible_connection: local

instructor:
  hosts:
    localhost:
  vars:
    ansible_connection: local

students:
  hosts:
    localhost:
  vars:
    ansible_connection: local

classroom:
  children:
    instructor:
    students:
