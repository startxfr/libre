---
local:
  hosts:
    localhost:
  vars:
    ansible_connection: local
    libre_type: instructor

instructor:
  hosts:
    localhost:
  vars:
    ansible_connection: local
    libre_type: instructor

bootstrap:
  hosts:
    localhost:
  vars:
    ansible_connection: local
    libre_type: bootstrap

students:

classroom:
  children:
    instructor:
    students:
