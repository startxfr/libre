---
local:
  hosts:
    localhost:
  vars:
    ansible_connection: local
    libre_type: instructor

boostrap:
  hosts:
    localhost:
  vars:
    ansible_connection: local
    libre_type: boostrap

instructor:
  hosts:
    localhost:
  vars:
    ansible_connection: local
    libre_type: instructor

students:

classroom:
  children:
    instructor:
    students:
